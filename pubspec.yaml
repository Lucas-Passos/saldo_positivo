name: saldo_positivo
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

# DEPENDÊNCIAS - BLOCO CORRIGIDO
dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.2
  intl: ^0.19.0
  fl_chart: ^0.66.0

  cupertino_icons: ^1.0.8 # Mantido
  # DUPLICATA REMOVIDA: hive: ^2.2.3
  # DUPLICATA REMOVIDA: hive_flutter: ^1.1.0
  path_provider: ^2.1.2   # Mantido

# DEV DEPENDÊNCIAS - BLOCO CORRIGIDO
dev_dependencies:
  flutter_test:
    sdk: flutter
  # DUPLICATA REMOVIDA: build_runner: ^2.4.6 (Mantemos a 2.4.7 abaixo)
  hive_generator: ^2.0.1 # Mantido

  flutter_lints: ^5.0.0
  
  # DUPLICATA REMOVIDA: hive_generator: ^2.0.1
  build_runner: ^2.4.7 # Mantido (É a versão mais recente/desejada)

flutter:
  uses-material-design: true